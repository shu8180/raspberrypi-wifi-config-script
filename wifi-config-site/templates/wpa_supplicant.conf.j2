ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
ap_scan=1
update_config=1

network={
        ssid="{{ ssid }}"
        scan_ssid=1
        psk="{{ pass }}"
        proto=RSN
        key_mgmt=WPA-PSK
        group=CCMP TKIP
}
